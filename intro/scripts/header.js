
// Get Ul element
alert(document.getElementById("test").href);


for (var i = 0; i < items.length; i++) 
{
    // Check if href = current url
    if(true || url.substring(url.lastIndexOf('/')+1) == "test")
    {
        // Change color of current link.
        alert("test");
    }
}